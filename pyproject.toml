[project]
name = "ha-homekit-heatercooler"
version = "0.1.0"
description = "Home Assistant custom integration to expose selected climate entities as HomeKit HeaterCooler"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"

[dependency-groups]
dev = [
  "ruff>=0.13.0",
]
